interface:
  display_name: "Measure Web Layouts"
  short_description: "Run deterministic Playwright layout captures"
  default_prompt: "Use $design-1-layouts-spec-with-playwright to run a deterministic layout capture for https://example.com at 1920x1080 and turn the JSON report into a parity spec."
