1) Overall assessment
Shell architecture: stable, but partly fragmented
The app has a clear dual-shell topology:
Platform shell: web/src/components/layout/AppLayout.tsx
Full-bleed route behavior is controlled in that same layout via route matching in isFullBleedRoute (flows, marketplace, workspace, parse/extract/convert/assets/schemas/superuser/test-integrations).
Flows-specific shell wrapper exists: web/src/components/layout/FlowsRouteShell.tsx
Admin shell is intentionally separate and not inside AppLayout: web/src/components/layout/AdminShellLayout.tsx
Routing confirms this separation and route ownership:
Platform routes under AppLayout: web/src/router.tsx
Admin routes under AdminShellLayout: web/src/router.tsx
Admin nav includes the capture page in a dedicated DESIGN section: web/src/components/admin/AdminLeftNav.tsx
Token/contract surface: present, but not yet consolidated as one canonical platform document
Token sources are real and explicit:
Colors: web/src/lib/color-contract.ts
Fonts: web/src/lib/font-contract.ts
Icons: web/src/lib/icon-contract.ts
Toolbar patterns: web/src/lib/toolbar-contract.ts
Shell layout/metrics: web/src/lib/styleTokens.ts
CSS variable authority: web/src/tailwind.css
Scalar host mapping: web/src/scalar-host/theme-contract.ts
Existing docs cover parts, but not a complete platform-shell contract:
App shell behavior docs exist for docs site, not platform app: web-docs/src/content/docs/internal/shell-contract-spec.md
Current config docs exist as fragments: web-docs/src/content/docs/internal/style-guide/current-configs/colors.mdx, web-docs/src/content/docs/internal/style-guide/current-configs/fonts.md, web-docs/src/content/docs/internal/style-guide/current-configs/layout.md
Layout capture + JSON spec generator: implemented but not yet an end-to-end platform contract loop
Capture flow exists and is wired into admin:
Capture UI: web/src/pages/superuser/DesignLayoutCaptures.tsx
Capture server: scripts/capture-server.mjs
Measurement script: docs/skills/design-1-layouts-spec-with-playwright/scripts/measure-layout.mjs
Platform-page spec pack tooling exists but is not directly fed from captures:
Package builder: docs/skills/design-4-extract-page-spec-contracts/scripts/build-platform-page-package.mjs
Audit report builder: docs/skills/design-3-spec-contract-based-design-audit/scripts/build-page-audit-report.mjs
Capture index is sparse (single marketing entry) and artifacts are not yet standardized at package scope: docs/design-layouts/captures.json
There is a known capture-systematic issue list with concrete fixes already documented, including theme handling, shell-shell row detection, selector uniqueness, and output path/backslash cleanup: docs/plans/2026-03-19-layout-capture-systematic-review.md
2) What to focus on next (prioritized)
1) Consolidate a single platform shell + token contract document
Create one canonical spec in web-docs for the platform app shell, with:
App shell modes (standard vs full-bleed)
Admin shell separation boundary
route-to-shell mapping
token references (source files + CSS variable names)
acceptance rules for token inheritance and non-shared values
2) Close capture pipeline gaps before scaling
Fix the listed known defects first (especially theme naming and shell-element detection), then make capture output transformation deterministic.
Normalize capture artifacts into the package builder input schema; right now capture metadata/report shape and package-builder input shape are not the same.
3) Run platform-wide shell/token consistency pass
Build a manifest of active /app routes from web/src/router.tsx, excluding admin routes for this cycle.
For each major route group, run capture + package + audit.
Store per-route findings + diffs so they become a review gate before merging.
4) Keep admin isolated as requested
Do not apply platform contracts to /app/superuser/* in the first pass.
Define an admin-specific contract if needed, but keep it separate so /app/superuser/design-layout-captures remains a tooling surface, not a production UX contract.
If you want, I can now generate the first canonical document scaffold in web-docs and include the exact shell/token matrix + route map as a starting contract.