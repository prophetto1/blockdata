import { SuperuserControlTowerPlaceholderPage } from './SuperuserControlTowerPlaceholderPage';

export function Component() {
  return (
    <SuperuserControlTowerPlaceholderPage
      title="CI/CD"
      summary="the purpose of this page is the same as the no 1 - to ensure visibility of the use of such throughout the system. In the same way, the platform must ensure that such are first registered as a registry so that existing CI/CD configs are visible and manageable through this page."
      testId="superuser-hook-system-placeholder"
      textOnly
    />
  );
}
