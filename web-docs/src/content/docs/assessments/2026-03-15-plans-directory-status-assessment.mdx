---
title: Plans Directory Status Assessment
description: Source-verified status of four documents in docs/plans/ — implementation progress, viability, and recommended actions.
---

# Plans Directory Status Assessment

- Date reviewed: `2026-03-15`
- Reviewer: `Codex`
- Files assessed: 4

---

## 1. FastAPI Execution Plane Systematic Buildout

**File:** `docs/plans/2026-03-15-fastapi-execution-plane-systematic-buildout.md`

**Type:** Architectural plan with implementation tasks

**Status: Partially Started — Phase 2 in progress, Phases 1/3/4 not started**

### What exists

| Deliverable | Status |
|-------------|--------|
| `services/platform-api/app/api/routes/plugin_execution.py` | Exists |
| `services/platform-api/app/api/routes/load_runs.py` | Exists |
| `services/platform-api/app/workers/conversion_pool.py` | Exists |
| `services/pipeline-worker/app/main.py` marked deprecated | Exists, confirmed deprecated |
| `docs/fastapi/fastapi-integration.md` | Exists (51 lines) |

### What does not exist

| Deliverable | Status |
|-------------|--------|
| Ownership matrix section in `fastapi-integration.md` | Missing (Phase 1) |
| `services/platform-api/app/api/routes/parse_runs.py` | Missing |
| `services/platform-api/app/api/routes/transform_runs.py` | Missing |
| `services/platform-api/app/workers/run_worker.py` | Missing |
| `services/platform-api/app/domain/runs/` directory | Missing |

### Assessment

The foundational pieces are real — FastAPI hosts plugin execution and load-runs. But the plan's primary deliverable (the ownership matrix and boundary documentation in Phase 1) has not been written. The plan itself effectively IS the ownership matrix but it hasn't been formalized into the docs.

**Recommendation:** This is an active architectural direction document. Keep in `plans/`. Phase 1 (documenting the boundary) is quick — the plan itself contains the content, it just needs to be moved into `docs/fastapi/fastapi-integration.md`. Parse/transform run routes and the worker primitives (Phases 2-3) are future work gated by product need.

---

## 2. Kestra Absorption Context Update

**File:** `docs/plans/2026-03-15-kestra-absorption-context-update.md`

**Type:** Context/purpose document (explicitly not an implementation plan)

**Status: Reference Material — No implementation to assess**

The document says on line 14: "It is not an implementation plan and not a feature spec." It is a context document preserving architectural decisions from the March 15 conversation.

### Key claims verified

| Claim | Verified |
|-------|----------|
| Registry tables exist in migration 050 (`service_registry`, `service_functions`, etc.) | Yes |
| Migration 054 extends with `project_service_config` | Yes |
| Integration catalog seeded (945 items) in migration 067 | Yes |
| Plugin satellite seed in migration 068 | Yes |
| Registry naming drift (`service_registry` vs `registry_services` in code) | Yes — confirmed in `admin-services/index.ts`, `services-panel.api.ts`, `ServicesCatalog.tsx` |

### Assessment

All factual claims are accurate. The document serves its stated purpose: preserving architectural intent. It has no implementation tasks to complete.

**Recommendation:** Move to a permanent reference location (e.g., `docs/architecture/` or `docs/context/`). It does not belong in `plans/` since it explicitly states it is not a plan.

---

## 3. Platform AI Provider Management

**File:** `docs/plans/2026-03-16-platform-ai-provider-management.md`

**Type:** Detailed implementation plan (6 tasks across database, backend, frontend)

**Status: 0% Implemented — Not started**

### Deliverable status

| Task | Deliverable | Status |
|------|-------------|--------|
| Task 1 | Migration `096_platform_api_keys.sql` | Does not exist |
| Task 2 | `admin_ai_providers.py` FastAPI route | Does not exist |
| Task 2 (prereq) | `crypto.py` with encrypt/decrypt | Exists (from load-activation) |
| Task 3 | `decryptPlatformApiKey` in `api_key_crypto.ts` | Does not exist |
| Task 3 | `getPlatformApiKey` in `assistant-chat/index.ts` | Does not exist |
| Task 4 | `SuperuserAiProviders.tsx` | Does not exist |
| Task 4 | `SuperuserProviderForm.tsx` | Does not exist |
| Task 5 | `SuperuserModelRoles.tsx` | Does not exist |
| Task 6 | Superuser routes in `router.tsx` | Not added |
| Task 6 | AI Providers in `SUPERUSER_DRILL` nav | Still in `SETTINGS_DRILL` |
| Task 6 | Settings gear removed from chat panel | Still present |
| Task 6 | Hardcoded "Sonnet 4.5" removed from chat | Still present |

### Current state

- `SettingsAiOverview.tsx` still exists and is routed at `/app/settings/ai`
- `SettingsProviderForm.tsx` still exists and is routed at `/app/settings/ai/:providerId`
- `SettingsModelRoles.tsx` still exists and is routed at `/app/settings/model-roles`
- AI Providers and Model Roles are still in the user-facing Settings nav
- Chat panel still has settings gear button and hardcoded model name

### Assessment

The plan is well-written with accurate file references and a clear dependency on the load-activation plan's `crypto.py` (which exists). The only prerequisite met is the crypto module. No implementation work has begun.

**Recommendation:** Keep in `plans/`. Ready to implement when prioritized. The plan's dependency on `crypto.py` is satisfied.

---

## 4. Ongoing Issues

**File:** `docs/plans/ongoing-issues.md`

**Type:** Living issue tracker

**Status: All 7 open issues still present — verified against code**

### Quick Fixes (all still present)

| Issue | File | Status |
|-------|------|--------|
| manage-overlays ownership check runs after mutation | `manage-overlays/index.ts` ~line 87 | Confirmed: RPC mutates before ownership check |
| trigger-parse nested query swallows errors | `trigger-parse/index.ts` ~lines 103-104 | Confirmed: error swallowed, defaults to `"__none__"` |
| export-jsonl test name/assertion mismatch | `export-jsonl/index.test.ts` ~line 142 | Confirmed: title says "normalized overrides", asserts `raw_docling_view` |
| Superuser document-views unreachable from nav | `nav-config.ts` SUPERUSER_DRILL | Confirmed: route exists in router.tsx but missing from nav |

### Resolves with Arango Hardening (all still present)

| Issue | File | Status |
|-------|------|--------|
| cleanup_outbox has no consumer | Entire codebase | Confirmed: zero functions read from cleanup_outbox |
| runs/index.ts missing cleanup_outbox write | `runs/index.ts` ~line 168 | Confirmed: catch block only logs, no outbox insert |

### Deferred (still present)

| Issue | File | Status |
|-------|------|--------|
| Transform routes drop project context | `router.tsx` lines 72-76 | Confirmed: `LegacyToTransform` discards projectId |

### Already resolved

| Issue | Status |
|-------|--------|
| Parse tab lost Docling JSON viewer | Marked resolved in doc, confirmed: DownloadsTab provides access |
| Migration 090 ADD COLUMN without IF NOT EXISTS | Forward-only, no action |
| Migration 092 DROP CONSTRAINT without IF EXISTS | Forward-only, no action |

### Assessment

The document is accurate and current. All open issues are real and verified. The Quick Fixes section contains ~4 items that are genuinely small (1-3 line changes each).

**Recommendation:** Keep in `plans/`. Consider knocking out the 4 Quick Fixes — they're each under 5 minutes of work.

---

## Summary

| Document | Type | Implementation Status | Recommendation |
|----------|------|----------------------|----------------|
| FastAPI Execution Plane | Architectural plan | ~20% (foundations exist) | Keep in plans — active direction |
| Kestra Absorption Context | Reference document | N/A (not a plan) | Move to `docs/architecture/` or `docs/context/` |
| Platform AI Provider Management | Implementation plan | 0% (not started) | Keep in plans — ready when prioritized |
| Ongoing Issues | Issue tracker | All items current | Keep in plans — consider quick fixes |