# UI (Next.js)

This folder is the Next.js frontend for the Writing System.

`ui/sample/` contains wireframes only.

## Local setup

1. Create `ui/.env.local` from `ui/.env.local.example`.
2. Install dependencies:

   - `npm install`

3. Run dev server:

   - `npm run dev`

